{
  "name": "javascript-packages",
  "version": "0.0.0",
  "devDependencies": {
    "ava": "0.25.0",
    "eslint": "4.18.1",
    "eslint-config-airbnb-base": "12.1.0",
    "eslint-config-prettier": "2.9.0",
    "eslint-plugin-import": "2.9.0",
    "eslint-plugin-prettier": "2.6.0",
    "husky": "0.14.3",
    "lerna": "^2.9.0",
    "lint-staged": "7.0.0",
    "mock-local-storage": "1.0.5",
    "prettier": "1.10.2",
    "sinon": "4.4.2"
  },
  "scripts": {
    "lint": "eslint --fix \"{,**,**/**}*.js\"",
    "build": "lerna run build",
    "test": "lerna run test",
    "update-dependencies": "npx npm-check-updates -u -a",
    "precommit": "npm run lint && npm test && npm run build && git add -A",
    "bootstrap": "lerna bootstrap"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix \"{,**,**/**}*.js\"",
      "git add"
    ],
    "*.json": [
      "prettier --write",
      "git add"
    ]
  }
}
